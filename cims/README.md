# CIM drop folder

Drop CIM PDFs here, then invoke the `falfurrias-cim-eval` skill.

PDFs in this folder are gitignored (CIMs are confidential). Only this README
and `.gitkeep` are tracked.

Naming convention: `<TargetCompany>_<Year>_<Banker>.pdf` (e.g. `Acme_2026_Houlihan.pdf`)
